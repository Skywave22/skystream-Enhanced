# This file configures the analyzer, which statically analyzes Dart code to
# check for errors, warnings, and lints.
#
# The issues identified by the analyzer are surfaced in the UI of Dart-enabled
# IDEs (https://dart.dev/tools#ides-and-editors). The analyzer can also be
# invoked from the command line by running `flutter analyze`.

# The following line activates a set of recommended lints for Flutter apps,
# packages, and plugin designed to encourage good coding practices.
include: package:flutter_lints/flutter.yaml

# Top-level `plugins:`, not `analyzer: plugins:` - the latter is the legacy
# plugin system the analyzer now warns about. riverpod_lint 3.x ships as an
# analysis_server_plugin, so declared here its rules run under `dart analyze`
# and in the IDE. NOT under `flutter analyze`, which does not load analyzer
# plugins - see the "Riverpod Lint" step in .github/workflows/ci.yml, which
# exists for exactly that reason. It was previously wired through custom_lint,
# which never loaded it at all, so none of these rules had ever run.
#
# Pinned exactly, not floated: the analysis server resolves this in its own
# plugin workspace, outside pubspec.lock, so a caret here means a future 3.x
# release can silently change which rules run with no commit in this repo.
plugins:
  riverpod_lint: 3.1.9

analyzer:
  language:
    strict-casts: true
    strict-inference: true
    strict-raw-types: true
  exclude:
    - "**/*.g.dart"
    - "**/*.freezed.dart"
    - "packages/**"
    - build/**
    - android/**
    - ios/**
    - web/**
    - windows/**
    - macos/**
    - linux/**

linter:
  rules:
    # Off, not skipped: analyzer 14 started applying this to private fields, and
    # all 19 sites it flags here initialise a private field from a NAMED
    # parameter - `SubDLProvider({String? apiKey}) : _apiKey = apiKey`. The
    # suggested fix, `{this._apiKey}`, is legal Dart (verified on 3.13: the
    # field is private but the external parameter name stays `apiKey`, so call
    # sites are unchanged). Kept off for churn, not correctness - turning it on
    # is a 19-site mechanical edit someone should make deliberately.
    prefer_initializing_formals: false
    # Core Lints
    avoid_print: true
    prefer_const_constructors: true
    prefer_const_declarations: true
    prefer_final_locals: true
    sized_box_for_whitespace: true
    use_key_in_widget_constructors: true
    prefer_const_literals_to_create_immutables: true
    unnecessary_string_interpolations: true
    # Architecture & Lifecycle Strictness
    use_build_context_synchronously: true
    unawaited_futures: true
    avoid_void_async: true
    prefer_final_in_for_each: true
    # discarded_futures: true  # 147 info-level sites; fix with unawaited() or await incrementally
#

# The `custom_lint` plugin above is riverpod_lint. All 15 of its rules
# (missing_provider_scope, notifier_extends, provider_dependencies,
# avoid_public_notifier_properties, avoid_ref_inside_state_dispose, ...) are
# enabledByDefault, so there is nothing to list here to switch them on - a
# `custom_lint: rules:` block only ever overrides a default, and any name not
# in it keeps its default (custom_lint_core LintRule.isEnabled). This file used
# to name five rules that riverpod_lint 3.1.0 does not ship
# (riverpod_avoid_global_provider, riverpod_avoid_read_inside_build,
# riverpod_avoid_watch_outside_build, riverpod_notifier_parameters,
# riverpod_async_notifier_error_handling); they were inert, and they read as if
# the rule set were narrower than it is. Add an entry here only to turn a real
# rule off, e.g.
#
#   custom_lint:
#     rules:
#       - provider_dependencies: false
#
# `flutter analyze` does not load analyzer plugins, so none of these rules run
# unless `dart run custom_lint` does. The CI workflow runs it on every push and
# pull request (.github/workflows/ci.yml, the "Custom Lint" step).
